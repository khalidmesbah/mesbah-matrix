import { Amiri, Amiri_Quran } from 'next/font/google';

export const AmiriFont = Amiri({
  weight: '400',
  subsets: ['arabic'],
});

export const AmiriQuranFont = Amiri_Quran({
  weight: '400',
  subsets: ['arabic'],
});
